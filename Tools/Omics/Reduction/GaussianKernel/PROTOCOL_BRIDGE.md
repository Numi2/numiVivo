# Preserve scalar compiler behavior

The underflow repair increased scalar-branch retired instructions on Hagai and
Kang from 104.0/780.2 billion to 141.8/1095.8 billion in the intermediate build,
while tiled instructions remained about 41.1/254.0 billion. Extract the nested
Swift/C pointer callbacks into a non-inlined helper, retaining all arithmetic,
resource settings, cancellation and fallback behavior. Rebuild and rerun the
complete protocol; retain both prior builds and their measurements.

Final timing claims must refer to the final executable. Do not treat a slowdown
of the scalar comparator as evidence of faster tiled execution. The final bridge
change must preserve every initial corrected score and anchor byte; biological
reuse remains conditional on complete payload identity. This is an implementation
repair without any biological parameter selection or altered acceptance margins.
