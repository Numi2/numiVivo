# Fixed record-writer qualification

Declared before running the candidate on complete Kang counts, base
`128c86801f630912e24c213418cb9c8ad7ae0bde`.

The retained owner was sampled during sixteen complete normalizations, eight
per backend. Every receipt matched the prior qualified full cohort. Those
instrumented calls identify CPU costs; they are not new speed measurements.

Replace the writer's per-record Swift Array appends with one privately owned,
initialized 1 MiB buffer. Preserve admission checks, pair encoding/order, hash
chunks, file synchronization, rejection behavior and all output bytes. This
changes no mathematical model, input filter or numerical precision profile.

Check zero, partial, exact and multiple buffer boundaries; UInt32 coordinate
limits and UInt64 payload bits; hash-only versus file mode; rejection without
state changes and overwrite protection. Check actual byte layout independently.

Run three rounds of full original Kang publication for each old/new owner and
CPU/Metal backend, with fixed rotated order. Check every resulting bundle against
the retained complete-cohort reference and run native reconstruction for the
new owner. All 24,673 cells, 15,706 features and 14,184,532 records remain in
scope. Use no model/output tuning. Report all timings and medians, including
negative results. Do not infer Metal superiority from a shared-writer gain.

Preserve raw samples, old/new source and executable identities, every receipt,
and independent checks. Reclaim generated duplicate payloads only after exact
hash and open-handle checks against retained originals. A passing count writer
does not establish biological prediction or completion of the broader roadmap.

## Safety review amendment before final-build execution

After the initial candidate comparison, review added an append-capacity guard so a failed flush cannot permit a later write beyond the fixed allocation. A closed-output test fills the buffer, observes the flush error, and requires the next append to reject. The final owner repeats the declared full-cohort comparison and CLI checks; initial-candidate observations remain separately archived. This is a memory-safety correction, not tuning from observed timing outcomes.
